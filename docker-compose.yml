version: '3.8'

services:
  app:
    build: .
    container_name: lecture_dashboard
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=dev
    env_file:
      - .env.dev
    restart: unless-stopped
    networks:
      - lecture-network

networks:
  lecture-network:
    driver: bridge
